You are implementing Phase {PHASE_NUMBER} of the FreeScout test expansion project.

REPOSITORY: Scotchmcdonald/freescout (branch: laravel-11-foundation)
WORKING DIR: /var/www/html
CURRENT STATUS: 674 tests passing, 49.78% coverage

YOUR ASSIGNMENT:
Read /var/www/html/docs/TEST_EXPANSION_PROPOSAL.md and find "PHASE {PHASE_NUMBER}" section for your specific tasks, components, and coverage targets.

FULL GUIDELINES:
Read /var/www/html/docs/AGENT_PRIMER_PROMPT.md for complete instructions, workflows, and examples.

CRITICAL RULES:
1. NO database migrations (shares DB with /var/www/html/archive/)
2. Always use RefreshDatabase trait
3. Always use factories for test data
4. Test behavior + database state, not just HTTP status
5. Run tests incrementally: php artisan test --filter=YourTest

QUICK START:
1. cd /var/www/html
2. cat docs/TEST_EXPANSION_PROPOSAL.md  # Find your Phase {PHASE_NUMBER}
3. php artisan test  # Verify 674 passing baseline
4. Review: cat app/YourComponent.php
5. Write tests in tests/Unit/ or tests/Feature/
6. Run: php artisan test --filter=YourTest
7. Commit when passing

TEMPLATE:
<?php
namespace Tests\Unit;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class YourTest extends TestCase
{
    use RefreshDatabase;
    
    public function test_behavior_description(): void
    {
        $model = Model::factory()->create();
        $result = $model->action();
        $this->assertTrue($result);
        $this->assertDatabaseHas('table', ['field' => 'value']);
    }
}

COMPLETION REPORT:
Phase {PHASE_NUMBER} Complete ✅
Tests Added: X | Coverage: Component XX%→YY% | All XXX tests passing

Begin by reading your Phase {PHASE_NUMBER} assignment in TEST_EXPANSION_PROPOSAL.md
